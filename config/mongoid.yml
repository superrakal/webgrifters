development:
  sessions:
    default:
      database: grifters_development
      hosts:
        - localhost:27017
      options:
  options:

test:
  sessions:
    default:
      database: grifters_test
      hosts:
        - localhost:27017
      options:
        read: primary
        max_retries: 1
        retry_interval: 0

production:
  sessions:
    default:
      database: grifters_production
      hosts:
        - localhost:27017
      options:
  options:
